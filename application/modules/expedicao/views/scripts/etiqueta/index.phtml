<script>
    $(document).ready(function(){
        $('#todos').live('click',function(){
            var checked_status = this.checked;
            $("input").each(function()
            {
                this.checked = checked_status;
            });
        });
    });
</script>

<h2>Expedição: <?php echo $this->expedicao ?></h2>
<div class="padding-top">

    <?php if ($this->cargas) : ?>
        <p><b>Selecione as cargas</b> - <input style="vertical-align: middle" type="checkbox" id="todos"> Selecione Todas<br></p>

        <div id="cargas">
            <?php foreach($this->cargas as $carga) : ?>
                <input
                       class="checkBoxClass"
                       style="vertical-align: middle"
                       type="checkbox"
                       name="cargas[]"
                       value="<?php echo $carga['codCargaExterno']?>"
                       >
                Carga <?php echo $carga['codCargaExterno']?>
            <? endforeach ?>
        </div>
    <?php endif; ?>

    <br><p><b>Selecione a central de entregas</b></p>
    <ul id="centrais">
        <?php foreach($this->centraisEntrega as $central) : ?>
        <li>
            <a href="<?php echo $this->url(array('module' => 'expedicao' ,'controller' => $this->controller, 'action' => $this->action, 'id' => $this->expedicao, 'central' => $central['centralEntrega']),'default',true) ?>">
            <img style="vertical-align: middle" src="<?php echo $this->baseUrl('img/icons/page_white_acrobat.png') ?>" alt="#" />
            Central <?php echo $central['centralEntrega'] ?>
            </a>
        </li>
        <? endforeach ?>
    </ul>
</div>