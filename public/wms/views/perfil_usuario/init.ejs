<h2>perfil_usuarios</h2>
<table cellspacing='0px'>
	<thead>
	<tr>
		<% for(var attr in Wms.Models.PerfilUsuario.attributes){%>
			<% if(attr == 'id') continue;%>
			<th><%= attr%> </th>	 
		<%}%>
		<th>Options</th>
	</tr>
	</thead>
	<tbody>
		<%= $.View('//wms/views/perfil_usuario/list',{perfil_usuarios: perfil_usuarios})%>
	</tbody>
</table>
<h2>New perfil_usuario</h2>
<form>
	<% for(var attr in Wms.Models.PerfilUsuario.attributes){ %>
		<% if(attr == 'id') continue;%>
		<div>
			<label><%= attr %> </label><br />
			<input type="text" value="" name="<%= attr %>"/>
		</div>
	<%}%>
	<input type='submit' value='Create'/>
</form>